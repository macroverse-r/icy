% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dot-read_yaml_config.R
\name{.read_yaml_config}
\alias{.read_yaml_config}
\title{Read YAML Configuration (Internal)}
\usage{
.read_yaml_config(
  package,
  type = c("local", "template"),
  section = "default",
  resolved_path = NULL,
  case_format = "snake_case",
  verbose = FALSE,
  resolved_template_path = NULL,
  config_data = NULL,
  template_types = NULL
)
}
\arguments{
\item{package}{Character string with the package name.}

\item{type}{Character string: "local" or "template".}

\item{section}{Character string for the section in the YAML file (default: "default").}

\item{resolved_path}{Pre-resolved path to the config file. If NULL,
the function will search for it using .find_config_files().}

\item{case_format}{Character string for filename generation.}

\item{verbose}{Logical. If TRUE, shows messages.}

\item{resolved_template_path}{Pre-resolved path to the template file.
Used for template type lookup when reading local configs.}

\item{config_data}{Pre-parsed YAML data. When provided, skips reading from disk.}

\item{template_types}{Pre-extracted template types list. When provided,
skips template file lookup for type resolution.}
}
\value{
Named list of configuration values.
For type "local", returns empty list if file does not exist.
For type "template", errors if file does not exist.
}
\description{
Unified low-level YAML reader for both local and template configuration files.
Reads the file, extracts a section, resolves variable references, and resolves
path variables using template types.
}
\details{
Used by get_config() as its internal reader, by check_conflicts() for standalone
config reading, by setup() for template inspection, and by .get_template_value()
for template metadata access.
}
\keyword{internal}
