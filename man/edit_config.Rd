% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit_config.R
\name{edit_config}
\alias{edit_config}
\title{Edit Configuration File}
\usage{
edit_config(
  package = get_package_name(),
  name = NULL,
  editor = "auto",
  validate = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{package}{Character string with the package name. Defaults to \code{get_package_name()} to detect the calling package.}

\item{name}{Optional character string for named configs (e.g., "gams_switches").
If NULL (default), opens the main config file (\{package\}_config.yml).}

\item{editor}{Character string specifying which editor to use:
\itemize{
\item "auto" (default): Use R's default \code{file.edit()}
\item "rstudio": Force RStudio editor if available
\item "vscode" or "code": Open in Visual Studio Code
\item "vim", "nano", "emacs": Terminal editors
}
Falls back to \code{file.edit()} if specified editor is unavailable.}

\item{validate}{Logical. If TRUE (default), validates YAML syntax after editing
and shows warnings for syntax errors. Does not prevent saving.}

\item{verbose}{Logical. If TRUE (default), displays informative messages about
the editing process.}
}
\value{
Invisibly returns the path to the edited file.
}
\description{
Opens the YAML configuration file for editing in your preferred editor.
This provides a better experience than just manually opening the file by
offering editor selection and YAML validation.
}
\examples{
\dontrun{
# Open config in default editor
edit_config("mypackage")

# Open in VS Code
edit_config("mypackage", editor = "vscode")

# Edit a named config
edit_config("mypackage", name = "gams_switches")
}

}
\seealso{
\code{\link{create_config}} for creating new config files,
\code{\link{get_config}} for reading config values
}
