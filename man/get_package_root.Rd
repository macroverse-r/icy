% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_config_dir.R
\name{get_package_root}
\alias{get_package_root}
\title{Get Package Root Directory}
\usage{
get_package_root(package = get_package_name())
}
\arguments{
\item{package}{Character string with the package name. Defaults to \code{get_package_name()}
to detect the calling package.}
}
\value{
Character string with the absolute path to the package root directory.
}
\description{
Returns the root directory of a package, handling both development
and installed package scenarios.
}
\details{
This function determines the actual package root directory:
\itemize{
\item In development: The current working directory (if it's a package directory)
\item When installed: The package installation directory
}
}
\examples{
\dontrun{
# Get the package root directory
pkg_root <- get_package_root("mypackage")

# Use it to locate package resources
data_dir <- file.path(pkg_root, "data")
inst_dir <- file.path(pkg_root, "inst")
}

}
